# HexaSortClone

### Структура и основные модули:

1.  **Core:** Содержит общие утилиты, менеджеры сцен, логику инициализации приложения и [другие низкоуровневые компоненты].
2.  **Services** Отвечает за логику состояний игры.
3.  **Scripts** Папка с компонентами отвечающими за представление
4.  **Map Editor:**

---

## a. Затраченное время ⏳

Общее время, затраченное на выполнение задания, составило **~50 часов**.

| Этап работы | Затраченное время (часы) |
| Создание редактора карт | ~4 часа |
| Создание первой версии прототипа/основной логики | ~16 часов |
| Рефакторинг, исправление багов и полишинг (финальная доработка) | ~30 часов |
| **ИТОГО** | **~50 часов** |

---

## b. Краткое описание архитектуры проекта 🏗️

Проект представляет собой реализацию игры-головоломки на **гексагональной сетке** (Sort Puzzle). Разработка выполнена на **Unity Engine** с использованием **C#**.
Используется модифицированный вариант MVC архитектурного паттерна
View модуль реагирует на изменение состояния получая информацию из модуля Controller через **события (Events/Actions)**
View модуль при получении инпута, или в ситуациях когда возможно изменение состояния, общается с модулем Controller с помощью команд, которые в процессе выполнения предварительно проводят валидацию

### Ключевые принципы и компоненты

1.  **Гексагональная Сетка (Hex Grid System):**
    * Реализована логика для работы с гексагональными координатами **Axial** для точного определения соседей и обработки взаимодействий.
    * Централизованное управление состоянием сетки и ее объектами (тайлами/фишками) осуществляется через один основной менеджер ILevelService
2.  **Разделение ответственности:**
    * Используется компонентно-ориентированный подход Unity, дополненный принципами **Single Responsibility** для игровых систем.
    * Логика игры (расчеты, условия победы) отделена от визуального представления и Input-систем.
    * Взаимодействие между независимыми системами организовано через **события C# (Events/Actions)** и **паттерн Command**.
3.  **Визуализация и анимация:**
    * Для всех анимаций движения элементов на поле, а также для плавных переходов UI, используется популярный плагин **DOTween**.
4.  **Редактор уровней (Map Editor):**
    * Для быстрого создания и тестирования уровней разработан кастомный инструмент в среде Unity Editor (**HexLevelEditorWindow**), позволяющий удобно размещать тайлы, настраивать стеки с элементами разных цветов, настраивать тип блокировки ячеек на игровой сетке.
    * Данные уровней, созданных в редакторе, хранятся в **ScriptableObjects**.

---

## c. Известные проблемы и возможные улучшения 🔧

### Известные проблемы (Known Issues)

* **Оптимизация:** На текущий момент могут наблюдаться небольшие накладные расходы при инициализации очень больших уровней или при массовом пересчете логики в одном кадре.
* **Удобство редактора:** В кастомном редакторе отсутствует функция **Undo/Redo** для быстрого отката изменений при создании карт.

### Возможные улучшения (Possible Improvements)

1.  **Улучшение архитектуры и качества кода:**
    * Внедрение паттерна **State Machine** для управления фазами игры (Initialization, Gameplay, Win/Lose) для повышения модульности.
2.  **Производительность:**
    * Использовать **Object Pooling** для элементов игровой сетки, чтобы сократить нагрузку на сборщик мусора (GC Allocations) во время активной игры.
3.  **Функциональность (Features):**
    * Добавить базовые настройки звука и музыки (SFX/BGM).
    * Реализовать систему **сохранения/загрузки** прогресса между сессиями.
